/**
 * 版本功能开关（FRONTEND-V1-PLAN §5）。
 * V1 = 纯数据形态：无图片/文档附件。B6（R2）落地时翻转为 true 即恢复全部入口。
 */
export const ATTACHMENTS_ENABLED = false;

/** V1.1 数据导出（后端 B8 端点就绪后翻转）。 */
export const EXPORT_ENABLED = false;
