# /etc/planet-api-owner.env —— 迁移（DDL）凭据，仅 deploy.sh source。
# 权限 0600 root:root；绝不能进 systemd 的 planet-api EnvironmentFile——
# 常驻服务进程（planet_app，无 DDL 权限）不应持有迁移密码。
PLANET_OWNER_PASSWORD=CHANGE_ME_OWNER
